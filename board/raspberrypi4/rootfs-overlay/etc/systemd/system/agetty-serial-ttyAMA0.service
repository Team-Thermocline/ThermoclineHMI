[Unit]
Description=Serial Getty on ttyAMA0
After=multi-user.target
# No BindsTo=dev-ttyAMA0.device so we don't block boot

[Service]
Type=simple
ExecStart=-/sbin/getty -L ttyAMA0 115200 vt100
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
