# Match rpi-image-gen templates/rpi/boot-firmware/config.txt (same as Raspberry Pi OS defaults).
# If you still see DISPLAY_DSI_PORT + reset loops with *unchanged* config, the VideoCore build
# on the card is suspect: replace start4.elf + fixup4.dat from the stable firmware tree (see README).

# For more options and information see
# http://rptl.io/configtxt
# Some settings may impact device functionality. See link above for details

# Uncomment some or all of these to enable the optional hardware interfaces
#dtparam=i2c_arm=on
#dtparam=i2s=on
#dtparam=spi=on

# Enable audio (loads snd_bcm2835)
dtparam=audio=on

# Additional overlays and parameters are documented
# /boot/firmware/overlays/README

# Automatically load overlays for detected cameras
camera_auto_detect=1

# Automatically load overlays for detected DSI displays
display_auto_detect=1

# Automatically load initramfs files, if found
auto_initramfs=1

# Enable DRM VC4 V3D driver
dtoverlay=vc4-kms-v3d
max_framebuffers=2

# Don't have the firmware create an initial video= setting in cmdline.txt.
# Use the kernel's default instead.
disable_fw_kms_setup=1

# Disable compensation for displays with overscan
disable_overscan=1

# Run as fast as firmware / board allows
arm_boost=1

# fw crypto lock permissions
lock_device_private_key=1

[pi02]
# If don't care about Bluetooth use the line below to get the pl011 instead of 8250 UART
# and use console=ttyAMA0,115200
# dtoverlay=disable-bt
enable_uart=1

[pi4]
enable_uart=1
# Put PL011 on GPIO 14/15 as /dev/ttyAMA0 (/dev/serial0); Bluetooth on mini-UART.
# Use disable-bt instead if you do not need Bluetooth at all.
dtoverlay=miniuart-bt
# Second PL011 for controller serial: UART2 on GPIO 0 (TXD2) / GPIO 1 (RXD2) — HAT ID pins; avoid HAT EEPROM conflict if unused.
# Device is often /dev/ttyAMA1 (enumeration can be ttyAMA2; see ls /dev/ttyAMA*). Thermocline Electron prefers AMA1/AMA2 first.
dtoverlay=uart2

[cm4]
# Enable host mode on the 2711 built-in XHCI USB controller.
# This line should be removed if the legacy DWC2 controller is required
# (e.g. for USB device mode) or if USB support is not required.
otg_mode=1

[pi5]
BOOT_UART=1

[cm5]
dtoverlay=dwc2,dr_mode=host

[all]
uart_2ndstage=1
